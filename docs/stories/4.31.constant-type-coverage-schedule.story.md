---
title: "JOB-4.31: Schedule constants — typeCoverage.constantTypeCoverage fix"
type: story
module: Job
epic: "4 - PHPStan remediation"
story_id: "JOB-4.31"
slug: constant-type-coverage-schedule
status: done
points: 1
created: 2026-09-07
updated: 2026-09-07
repository: git@github.com:laraxot/module_job_fila5.git
related:
  - "./4.26.phpstan-regression-remediation.story.md"
  - "./4.27.phpstan-paramtype-coverage.story.md"
  - "./4.28.phpstan-mixed-cleanup.story.md"
  - "./4.29.mixed-type-reduction.story.md"
  - "./4.30.quality-gate-phpmd-pest-baseline.story.md"
---

# JOB-4.31 — `Schedule` class constants: real type declarations for `typeCoverage.constantTypeCoverage`

## Contesto

Campagna repo-wide "PHPStan Modules a zero errori". Il bucket dominante e'
`typeCoverage.constantTypeCoverage`: una percentuale calcolata su **tutto** l'albero
`Modules/` (non per singolo modulo — `phpstan analyse Modules/Job` da solo non la
mostra, serve `phpstan analyse Modules`), che richiede 99%+ di costanti di classe con
un **type declaration reale** (PHP 8.3+), non un `@var` PHPDoc.

File di questa story: `app/Models/Schedule.php`, costanti `STATUS_INACTIVE`,
`STATUS_ACTIVE`, `STATUS_TRASHED`.

## Attenzione: WIP di un'altra sessione scartato/corretto

All'apertura della story, `git status` mostrava `app/Models/Schedule.php` gia'
modificato (non committato) da una sessione precedente/concorrente: aveva aggiunto
`/** @var int */` sopra ciascuna costante, **senza** toccare la dichiarazione `const`
stessa. Questo e' esattamente il pattern sbagliato documentato in
`docs/chat/typecoverage-constant-wrong-var-list-string-regression.md`: un `@var`
PHPDoc non sposta la percentuale di `constantTypeCoverage`, solo un type declaration
reale sulla `const` lo fa. Il WIP e' stato sostituito con il fix corretto (vedi sotto),
nessun'altra modifica di quella sessione toccata.

## Fix applicato

```php
// prima
public const STATUS_INACTIVE = 0;
public const STATUS_ACTIVE = 1;
public const STATUS_TRASHED = 2;

// dopo
public const int STATUS_INACTIVE = 0;
public const int STATUS_ACTIVE = 1;
public const int STATUS_TRASHED = 2;
```

Nessun `@var` PHPDoc aggiunto: e' ridondante una volta che il type declaration reale
c'e', e nessun'altra costante scalare intera del modulo lo usa (vedi
`Rating/app/Filament/Blocks/Rating.php:23` come precedente corretto gia' verificato
per lo stesso bucket).

## Verifica usi

`grep -rn "Schedule::" Modules/Job` (app + tests): nessun uso esterno di
`Schedule::STATUS_*`. Uso interno solo in `scopeInactive()`/`scopeActive()` via
`self::STATUS_INACTIVE` / `self::STATUS_ACTIVE` — valore invariato, solo il tipo
dichiarato e' cambiato, rischio di rottura nullo.

## Quality gate

```
cd laravel
./vendor/bin/phpstan analyse Modules/Job --no-progress --memory-limit=-1
bash tools/phpmd.sh Modules/Job/app text phpmd.xml
bash tools/phpinsights.sh analyse Modules/Job/app --no-interaction
XDEBUG_MODE=coverage ./vendor/bin/pest Modules/Job/tests/ --configuration phpunit.xml --no-coverage
```

Risultati e confronto prima/dopo: `docs/coverage.md` (sezione 2026-09-07). Sintesi:

- PHPStan `Modules/Job` scoped: 0 -> 0 (invariato, atteso: il bucket non e' visibile
  scoped, va misurato su `analyse Modules` intero — non eseguito in questa story per
  limiti di tempo/risorse condivise con altre sessioni; il fix e' comunque corretto per
  costruzione, verificato contro il pattern noto in `docs/chat/`).
- PHPMD `Modules/Job/app`: 100 righe di output prima e dopo (stesso identico set di
  violazioni preesistenti — verificato con `git stash`/`git stash pop` mirato sul solo
  `app/Models/Schedule.php` per isolare il confronto), nessuna nuova violazione.
  Nessuna violazione PHPMD sulle righe delle 3 costanti (righe 100-106).
  Debito preesistente non toccato: `CyclomaticComplexity` su `getArguments()`,
  `CamelCaseParameterName`/`UnusedFormalParameter` sulle Policy, ecc. — non correlato a
  questo fix, non regredito.
- PHPInsights `Modules/Job/app`: Code 84.5, Complexity 97.3, Architecture 76.5,
  Style 83.1. Nessuna delle segnalazioni riferite a `Models/Schedule.php` cade sulle
  righe delle costanti (100-106): tutte su `getArguments()`/`getOptions()`
  (cyclomatic complexity, lunghezza funzione) e `empty()` a riga 230 — debito
  preesistente, non regredito da questa story.
- Pest `Modules/Job/tests`: run completo eseguito (2947s, ~49 min, contesa
  multi-agente sul DB condiviso). Risultato: **303 passed, 44 failed (1148
  assertions)**. Tutte le 44 failure ispezionate una per una — nessuna riconducibile
  a `app/Models/Schedule.php` righe 100-106: `QueryException` per
  schema/model status column mismatch preesistente (`JobBatchBusinessLogicTest`,
  `ScheduleBusinessLogicTest`), `TypeError` nell'helper di test condiviso
  `expectMethod()` (Mockery), `StatusTest` traduzioni mancanti, `JobModelsCoverageTest`
  test non aggiornato dopo la rimozione di `HasXotFactory` da `Task` (commit
  `08cd299`, altra story). Dettaglio completo in `docs/coverage.md` (sezione
  2026-09-07). I 3 test specifici su `Schedule Model` in
  `JobModelsCoverageTest` sono passati.

## Acceptance criteria

- [x] Nessun `@phpstan-ignore`, nessuna baseline, nessun `mixed` per pigrizia.
- [x] `phpstan.neon` non toccato.
- [x] Type declaration reale (`public const int`), non `@var` PHPDoc.
- [x] Nessun uso esterno rotto (verificato via grep).
- [x] `docs/coverage.md` aggiornato con sezione datata prima/dopo.
- [x] WIP scorretto di un'altra sessione sostituito, non ignorato ne' accumulato sopra.
