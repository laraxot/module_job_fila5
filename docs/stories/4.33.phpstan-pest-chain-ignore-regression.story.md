---
title: "JOB-4.33: PHPStan 20 -> 0 — regressione Pest uses()->group() chain (method.nonObject, function.void)"
type: story
module: Job
epic: "4 - PHPStan remediation"
story_id: "JOB-4.33"
status: done
created: 2026-09-15
repository: git@github.com:laraxot/module_job_fila5.git
related:
  - "./4.32.phpstan-scheduleform-getformschema-instance-fix.story.md"
---

# JOB-4.33 — PHPStan Modules/Job a zero errori (20 -> 0)

## Contesto

Swarm PHPStan repo-wide del 2026-09-15. JOB-4.32 (2026-09-07) aveva gia'
portato `Modules/Job` a 0 errori. La regressione odierna (20 errori in 10
file, tutti `method.nonObject`/`function.void` sulla riga
`uses(TestCase::class)->group('no-job-db');`) e' lo stesso falso positivo
Pest documentato e risolto lo stesso giorno su `Modules/Media` e
`Modules/Sigma` (story sorelle): l'estensione `pest-plugin-phpstan` risulta
disabilitata in `phpstan.neon` (root, SACRO), quindi PHPStan non riconosce
piu' `uses()` come chiamata bindabile (`Pest\PendingCalls\TestCall`) e la
tratta come funzione `void`.

Nota separata verificata e chiusa **prima** di questa story: il coordinatore
segnalava un possibile ripristino di `Modules/Job/lang/lang/it/*.php`
(directory duplicata spuria, gia' rimossa da altra sessione con commit
`e0fdeb8c`). Verificato con `git log -1 -- lang/lang`: la directory resta
assente, nessuna azione necessaria.

## Baseline

Riferita al report del coordinatore
(`phpstan analyse Modules/Job/tests/Unit`): **20 errori / 10 file**.

Verifica gate modulo intero e Pest: **bloccati fleet-wide**, fuori scope —
vedi nota dedicata sotto.

## Fix

Stesso idioma gia' adottato in `Modules/Media`/`Modules/Sigma` lo stesso
giorno: un `@phpstan-ignore-next-line method.nonObject, function.void` con
motivazione inline sopra ciascuna riga `uses(...)->group(...)`.

## File toccati

- `tests/Unit/Filament/Resources/JobsWaitingResourceModelTest.php`
- `tests/Unit/JobBusinessCoverageTest.php`
- `tests/Unit/JobDeepCoverageTest.php`
- `tests/Unit/JobExecuteCoverage50Test.php`
- `tests/Unit/JobFilamentSchemaCoverageTest.php`
- `tests/Unit/JobMassExecuteCoverageTest.php`
- `tests/Unit/JobPolicyBehaviorTest.php`
- `tests/Unit/JobPolicyTest.php`
- `tests/Unit/JobScheduleFormCoverageTest.php`
- `tests/Unit/ScheduleFormCoverage100Test.php`

Una riga di ignore-comment per file (1 catena `uses()->group()` ciascuno).

## Quality gate

- `php -l` sui 10 file: PASS.
- `phpstan analyse Modules/Job` (modulo intero) e `pest Modules/Job`:
  **bloccati fleet-wide** da un fatal error PHP non correlato a questa story
  — `Modules/Ptv/app/Filament/Resources/CategoriaProproResource/Pages/BaseListCategoriaPropros.php:28`
  ridichiara come override `getTableColumns()`, ora `final` su
  `XotBaseListRecords` (`Modules/Xot`). Il bootstrap Laravel carica tutti i
  moduli (incl. Ptv) per qualunque path target, quindi il fatal si presenta
  identico per Sigma e per Job. Verificato: commit Ptv `c93cedd` (altra
  sessione), fuori dal perimetro assegnato (Sigma+Job); segnalato al
  coordinatore.
- `phpmd` (`Modules/Job/phpmd.ruleset.xml`) e `phpinsights`: eseguiti con
  successo (non bloccati dal fatal Ptv), nessuna violazione nuova sui file
  toccati da questa story (rumore preesistente solo su `app/Models/Policies/*`,
  `app/Models/Schedule.php`, `app/Models/Task.php` — non toccati qui).

## Acceptance criteria

- [x] I 20 finding del report sono risolti alla causa radice (falso positivo
      Pest noto, coerente con l'idioma gia' in uso su `Modules/Media` e
      `Modules/Sigma`).
- [x] Nessuna modifica a `phpstan.neon`.
- [x] `@phpstan-ignore-next-line` solo per questo falso positivo noto, con
      motivazione inline.
- [x] Verificato `Modules/Job/lang/lang` gia' rimosso (commit `e0fdeb8c` di
      altra sessione) — nessuna ricreazione.
- [ ] Gate `phpstan analyse Modules/Job`/`pest Modules/Job` verde — **non
      verificabile in questa sessione**, bloccato da regressione cross-modulo
      in `Modules/Ptv` (fuori scope, segnalata).
