# Story 1.1 — Profile Model Reference Fix + Job Module Casting

**Status:** In Progress (defer to next session)
**Epic:** PHPStan L10 → 0 errors global
**Owned File/Module Scope:** `Modules/Job`

## Objective
Resolve 104 PHPStan errors in Job module, concentrated in:
- Job model casting `mixed → int` (lines 84, 85, 95, 106, 124, 134, 135)
- Job model type coverage for constants
- Job model related methods type inference

## Problem Statement
Session prior fixed 6 PHPStan errors in Xot+Media, reaching 0 errors in those modules.
Final global scan revealed 104 errors concentrated in Job and Lang modules, indicating:
1. **Lang issues**: Resolved in this session (Profile model FQCN refs → `\Modules\Xot\Contracts\ProfileContract`)
2. **Job issues**: Pending. Casting errors on mixed types, likely from:
   - Job model properties without explicit casts
   - Eloquent relationship type inference gaps
   - Constants not typed

## Solution Sketch
1. Audit Job model schema: inspect columns, relationships, constants
2. Add explicit casts to Job model for columns being cast as int
3. Type hint related methods (scopes, accessors)
4. Verify type coverage improvements
5. Commit Job module + sync git

## Done When
- Job module PHPStan: 0 errors
- phpmd clean
- pest passes (coverage ≥ prior baseline)
- git commit + all remotes synced
- BMAD story file closed (no pending reqs)

## Continuation Notes for Next Session
- Verify Job model definition: `Modules/Job/app/Models/Job.php`
- Check $casts array and relationship type hints
- Run: `./vendor/bin/phpstan analyse Modules/Job --memory-limit=-1` to see errors
- Profile fix (Lang) ready to integrate if other agents edit those files

---

**Blocked By:** None (Lang fix complete)
**Blocks:** Global PHPStan run to confirm 0 errors
**Estimated Effort:** Medium (type hints + cast audit)
