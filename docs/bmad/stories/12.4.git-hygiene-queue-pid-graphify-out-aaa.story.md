---
id: "12.4"
epic: 12
story: "4"
title: "Job: untrack queue.pid/graphify-out, rimozione .aaa morti, verifica marker README/contributing"
slug: git-hygiene-queue-pid-graphify-out-aaa
status: done
scope: module:Job
created: 2026-09-22
updated: 2026-09-22
qmd: "job queue.pid graphify-out aaa dead files gitignore conflict markers concurrent agent"
related:
  - ./12.3.root-hygiene-conflict-markers.story.md
  - ../../coverage.md
  - ../../../../Xot/docs/bmad/stories/graphify-out-gitignore-all-modules.story.md
---

# 12.4: igiene git residua (queue.pid, graphify-out, .aaa) + incidente agente concorrente

**Perché.** Incarico esplicito: ripulire i residui del rebase abortito su `dev`
(`git reflog`: `rebase (abort): returning to refs/heads/dev`, HEAD@{1}→{0} `2f146e72`→
`aa0956f3`). Elenco atteso: marker annidati in `README.md`/`.github/contributing.md`,
due file `.aaa` sospetti, `queue.pid` tracciato, 8 file `graphify-out/` tracciati
nonostante `.gitignore` (riga `graphify-out`, funzionalmente equivalente a
`graphify-out/`, non duplicata).

## Incidente: collisione con agente concorrente (evidenza)

Appena iniziato l'intervento, `git diff README.md` è fallito con "File has been
modified since read" — il working tree stava cambiando sotto i piedi. Verifica:

```
$ cat laravel/Modules/Job.lock
LOCKED at 2026-09-22T18:14:33+02:00
Task: fix-rebase-conflict-job-module
Agent: unknown

$ cat laravel/Modules/Job/README.md.lock   # (presente alle 18:18, rilasciato ~18:23)
LOCKED at 2026-09-22T18:18:23+02:00
Task: job-12.3-hygiene
Agent: cursor-grok
```

Un secondo agente (`cursor-grok`, via il lock-holder "unknown" di `Job.lock`) stava
eseguendo **lo stesso identico task** in parallelo, ed è l'autore reale della story
[12.3](./12.3.root-hygiene-conflict-markers.story.md) (marker README/contributing,
rimozione `ARCHITECTURE.md`/`Untitled`/backup datati/`lang/it/.php`, PHPStan verificato
`[OK] No errors`). Applicando lo standing order "non forzare, segnala e salta" (pilastro
4), questa story **non duplica** il lavoro di 12.3: si limita a (a) verificare
indipendentemente il risultato di 12.3 sui due file marker, (b) coprire gli item **non
in scope di 12.3** — `queue.pid`, `graphify-out/`, `.aaa`.

Per evitare l'`index git condiviso` (memoria `feedback-git-index-is-shared-between-agents`),
non è mai stato usato `git add -A`/`commit -a`: solo `git add <path>` mirato sui file di
competenza di questa story, lasciando intatti nel working tree i file ancora
unstaged dell'altro agente (`docs/bmad/*`, backup, `ARCHITECTURE.md`, nuove story).

## Verifica indipendente marker (non duplicare il lavoro, verificarlo)

```
$ grep -cE '^(<{7}|={7}|>{7})' README.md .github/contributing.md
README.md:0
.github/contributing.md:0
$ grep -c '^```' README.md .github/contributing.md   # parità fence
README.md:2
.github/contributing.md:0
```

Contenuto confrontato con il template standard già adottato in Xot/Activity/Tenant
(frontmatter `id/title/type/.../issues/discussions`, sezioni `## Confini
architetturali` → `## Integrazione rapida` → `## Documentazione` → `## Qualità e
manutenzione`, chiusura `**Modulo** \`xxx\` · **Laraxot ecosystem** ·
**Project-agnostic**`): la risoluzione di `cursor-grok` è coerente con la convenzione
di repo, non un "tieni sempre HEAD" meccanico — verificato riga per riga contro
`git show HEAD:README.md` (3 livelli di marker annidati, doppio merge non risolto
prima di essere ricommittato, coerente con la firma nota del daemon auto-commit,
memoria `project-local-autocommit-daemon-per-module.md`).

## `.aaa` — evidenza per la rimozione (non rinomina)

```
$ git log --all --diff-filter=A --oneline -- '*MonitoredScheduledTask*' | tail -1
b5fb9107 .   # commit INIZIALE del repo (2026-01-20), file già .aaa da subito
$ grep -rn "MonitoredScheduledTask" app/ database/ tests/ config/ routes/ | grep -v '\.aaa:'
(nessun risultato — zero riferimenti fuori dai due file stessi)
$ grep -n "schedule-monitor" ../../composer.json ../../composer.lock
(nessun risultato — dipendenza spatie/laravel-schedule-monitor non installata)
```

Contenuto: classi stub (`class MonitoredScheduledTask extends BaseModel {}`) col vero
`extends \Spatie\ScheduleMonitor\Models\...` commentato. **Non esiste evidenza che il
nome originale fosse `.php`** — contrariamente all'ipotesi di partenza, la storia mostra
`.aaa` fin dal commit iniziale. Verdetto: codice morto/mai attivato (dipendenza
assente, zero riferimenti, mai valido come `.php`) → **rimozione** (`git rm`, non
`--cached`), non rinomina — rinominare avrebbe "reinventato" un file mai esistito come
`.php` funzionante.

## Fix applicati (commit separato da 12.3)

1. `git rm --cached app/Console/Commands/queue.pid` + `*.pid` in `.gitignore` (assente
   prima; il file conteneva un PID runtime, `9252`, mtime coincidente con l'avvio
   dell'agente concorrente — probabilmente rigenerato da un worker in esecuzione).
2. `git rm --cached` sugli 8 file `graphify-out/` tracciati (restano su disco,
   rigenerabili con `graphify update .`); `.gitignore` riga 152 `graphify-out` lasciata
   com'è (equivalente a `graphify-out/`, nessuna duplicazione).
3. `git rm app/Models/MonitoredScheduledTask.aaa app/Models/MonitoredScheduledTaskLogItem.aaa`
   (rimozione reale, evidenza sopra).
4. Verifica indipendente marker README/contributing (nessuna modifica necessaria,
   già corretti da 12.3) + staging nello stesso commit per non lasciarli esposti a
   un futuro `dummy_push()`/merge automatico del daemon (memoria
   `project-local-autocommit-daemon-per-module.md`).

## Gate qualità

- PHPStan `analyse Modules/Job` → `[OK] No errors` (dopo la rimozione dei due `.aaa`;
  nessun impatto perché mai autoloadati).
- PHPMD (`tools/phpmd.sh Modules/Job`): 124 righe di finding, debito pre-esistente,
  non toccato (fuori scope, rischio collisione). Dettaglio in
  `../../coverage.md`.
- PHPInsights: CODE 90.6, COMPLEXITY 100, ARCHITECTURE 71.4, MISC 87.7. Debito
  pre-esistente, non toccato. Dettaglio in `../../coverage.md`.
- Pest: DB `10.100.200.53:3306` **non raggiungibile** (`nc -z -w3` fallito prima del
  lancio) → suite in hang, terminata da timeout esterno. Nessun coverage number
  producibile in questa sessione (nessun baseline pre-esistente in `docs/coverage.md`,
  che non esisteva — creato ora con stato onesto).

## Push

`git push laraxot dev` -> `aa0956f3..0e6bf4ff  dev -> dev` (fast-forward, riuscito,
0 tentativi di force/pull/rebase/merge). Al momento del push il branch remoto era
allineato (0 ahead/0 behind) — la divergenza "1990 commit" del contesto iniziale
era stale, superata dall'attivita' dell'agente concorrente nel frattempo.

## Fuori scope (annotato, non toccato)

- Debito PHPMD/PHPInsights pre-esistente (124 finding / punteggi 90.6-71.4): non è
  git-hygiene, richiederebbe una story dedicata e collide col lock di modulo attivo.
- Contenuto di `docs/bmad/livewire-inventory.md` e affini: proprietà dell'altro agente
  (story 12.3/12.1), non verificato né toccato qui.

## Dev Agent Record

- Commit: `0e6bf4ff` "fix(Job): risolvi marker di conflitto annidati residui
  (README/contributing), rimuovi queue.pid tracciato, rimuovi file .aaa morti,
  untrack graphify-out/" (2026-09-22T18:27:54+02:00).
- Staging mirato (mai `-A`/`-a`): `.gitignore`, `app/Console/Commands/queue.pid`
  (rimosso dall'indice), `app/Models/MonitoredScheduledTask.aaa` +
  `MonitoredScheduledTaskLogItem.aaa` (rimossi), `README.md`, `.github/contributing.md`
  (verificati, gia' risolti da 12.3/cursor-grok), 8 file `graphify-out/` (rimossi
  dall'indice, mantenuti su disco).
- Push: `git push laraxot dev` -> `aa0956f3..0e6bf4ff  dev -> dev`, fast-forward,
  nessun rifiuto, nessun force. Verifica post-push: `git status -b` ->
  `dev...laraxot/dev` senza ahead/behind (0/0).
- Working tree residuo dopo il mio commit: modifiche NON mie, in corso dall'agente
  concorrente (story 12.3): `.gitignore` (ulteriore riga), delete di `ARCHITECTURE.md`,
  `README.md.backup_20251210_092006`, `Untitled` — non toccate, fuori dal mio staging.
- Story madre `../../../Xot/docs/bmad/stories/graphify-out-gitignore-all-modules.story.md`:
  **nessuna tabella "Esito swarm" ne' cella "BLOCCATO" trovata nel file** (35 righe
  totali, verificato per intero) — la premessa del task su questo punto era stale/errata,
  come gia' accaduto per la stima "1990 commit divergenti". Il file ha solo una sezione
  "Stato" testuale (`in-progress -> vedi docs/sprint-status.yaml`); il file NON e' stato
  modificato per evitare di inventare contenuto assente. `docs/sprint-status.yaml` riga
  1543 (`Xot/graphify-out-gitignore-all-modules`) e' gia' `done` con nota aggregata
  "zero mancanti e zero file tracciati" — coerente ora anche per Job dopo questo commit.
