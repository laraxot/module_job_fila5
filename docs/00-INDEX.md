# Job — Index

Bridge stub (case-duplicate of the canonical file). Read
[00-index.md](./00-index.md) instead — this uppercase variant existed only
because of an unresolved merge and drifted out of sync (e.g. it linked to a
non-existent `readme.md`).
